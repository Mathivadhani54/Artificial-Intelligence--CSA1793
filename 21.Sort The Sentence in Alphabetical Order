def sort_sentences(text):
    sentences = text.split('. ')
    sorted_sentences = sorted(sentences)
    return '. '.join(sorted_sentences)
text = "This is a sentence. Here is another one. Sorting sentences is fun."
sorted_text = sort_sentences(text)
print(sorted_text)
